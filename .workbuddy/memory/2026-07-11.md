# 2026-07-11

## 千摊智脑 · 代码质量评审 + 团队技术提升方案（资深开发工程师视角）
- 评审对象：`qiantan-brain/backend`（FastAPI）+ 既有《代码质量与团队能力提升指南》。
- 核心发现（已附 file:line 证据）：
  - **P0-1 鉴权缺位（系统性）**：`merchant_id` 由客户端任意指定，全站无 auth，横跨 11 个路由 25+ 处接口签名；最恶劣在 `advice.py:222` 缺失时随机生成 merchant_id。`main.py` 12 个 router 无 auth 路由。
  - **P0-2 CORS**：`main.py:33-39` `allow_origins=["*"]` + `allow_credentials=True` 自相矛盾。
  - **P1-1**：`response_model=dict` 绕过团队自有的 `DailyAdviceResponse` schema（写了没用）。
  - **P1-2**：`simulate_scenario`(`advice.py:216`) 回归裸 `dict` 入参，同文件 `simulate_what_if_endpoint` 却用 `WhatIfRequest`，风格不一致。
  - **P2-1 Fat Router**：`get_daily_advice`(`advice.py:28-171`) 约 80 行内联编排。
  - **P2-2 N+1**：商品循环里每商品发 3 条查询。
  - **P3-1**：`advice.py:114` 静默吞掉预测异常。
- 团队已有基础（加分）：Pydantic schema 规范、13 个 pytest 文件、`pyproject.toml` 已配 ruff+mypy、Alembic 已接入（`migrations/versions/001_*.py`）。问题在"有配置没强制" + `main.py` 仍 `create_all` 启动建表（与 Alembic 双轨）。
- 交付物：
  - `E:\千摊\千摊智脑-代码质量评审与团队提升方案.md`（分级评审 + 4 阶段提升路线 + 行动清单）。
  - `E:\千摊\千摊智脑-auth参考实现.py`（`get_current_merchant` 依赖 + 改造后 `simulate_scenario` + 瘦路由拆分示例，标注为参考代码待 review）。
- 下一步建议：本周立项 G1 鉴权（微信登录+JWT+`get_current_merchant`），启用 pre-commit(ruff+mypy)，CI 加"禁止裸 dict / 禁止客户端传 merchant_id"规则。

## 已落地重构（用户确认"改" → 按参考实现改造 advice.py 两接口）
- 改动文件：
  - `app/schemas/advice.py`：扩展 `ScenarioInput`（加 product_name/estimated_sales_base/avg_historical_price），新增 `ScenarioBatchRequest`、`ScenarioResponse`（信封{code,data}+data 内层 `SimulationOutput` 强类型）；注意 Pydantic 前向引用——`ScenarioResponse` 必须定义在 `SimulationOutput` 之后。
  - `app/services/advisor.py`：新增 `async build_daily_advice(db, merchant_id)`，把原 `get_daily_advice` 路由里 ~80 行内联编排（环境/商品/库存/7-30日销量/预测/个性化/落库）搬入 service；顶部补 `sqlalchemy/模型/behavior/forecast` 导入，去除函数内重复 import。
  - `app/routers/advice.py`：重写为瘦路由——`get_daily_advice` 仅 `build_daily_advice` 封装；`simulate_scenario` 改裸 dict → `ScenarioBatchRequest`，**删除"merchant_id 缺失则随机生成"的危险逻辑**，响应走 `ScenarioResponse`。
- 关键约束（未破坏）：保留 `merchant_id` 查询参数（小程序与测试仍以此传身份），保留 `{code,data}` 信封；G1 鉴权落地前不在这两个路由硬接 `get_current_merchant`，仅在代码里标 TODO 指明替换点（见 `千摊智脑-auth参考实现.py`）。
- 验证：`.venv` 跑 `pytest` 全量 **94 passed**（含 advice/simulator/unit）；`py_compile` 通过。
- 教学点：本次演示了"裸 dict→强类型"、"Fat Router→service 编排"、"删危险默认身份"三件事，可作为团队 code review 样板。

## 续：真实账本基座落地（catalog / 幂等 / 单位换算 / 语音v2 / 库存聚合 / 迁移 / 路线图）
- 修复 `app/services/voice_parser_v2.py` 的 `CN_NUM` 命名 bug（`_CN_NUM`→`CN_NUM`），10 个单测全绿。
- 新增 `app/models/catalog.py`：ProductSKU / ProductAlias / ProductSpecification / Unit / UnitConversion / Supplier / SupplierProduct 七张表（向后兼容不改动旧表），注册进 `app/models/__init__.py`；全量 94 测试通过。
- `app/models/inventory.py:38` 加 `idempotency_key`（unique + index，NULL 不参与冲突）。
- 新增 `app/services/unit_service.py`（内置重量因子 + DB 换算）+ 测试（4）；`app/services/voice_parser_v2.py`（BusinessEvent + 可注入 CatalogResolver：提取供应商 / 包装与净重分离 / 单斤成本 / 幂等键 / needs_confirmation）+ 测试（6）。
- 修复 `app/routers/inventory.py:get_current_inventory`：去 `.limit(200)` 截断、排除 `is_voided`、改单条 SQL `SUM/GROUP BY` + 加权均价（`case` 条件聚合）。
- 修复 `migrations/env.py`：硬编码模型导入遗漏 stocktake/purchase/audit/catalog → 改 `from app.models import *`（与 `__all__` 自动同步，杜绝漏迁移）。
- 生成 `migrations/versions/002_catalog_and_idempotency.py` + `migrations/catalog_schema.sql`（等价迁移：7 新表 + idempotency_key 列/索引）。
- `scripts/_sync_dev.py` 同步 dev 库：`create_all` 补 catalog 表 + 手动补 `idempotency_key` 列（因 `create_all` 不ALTER已有表）；验证 22 张表齐全。
- `team-code-quality-guidance.md` §三.5 新增「后端账本专项红线」7 条 + Review 清单 6 条 + env.py 通配导入说明。
- 产出 `E:\千摊\千摊智脑-技术评审与提质路线图.md`（strengths + P0 缺口 file:line 证据 + 三阶段任务卡 + 最优先5功能 + 取舍 + 团队提升建议）。
- 待续（未做）：P0-1 微信登录/JWT 仍未落地；供应商/应收应付账本未建表；AI 建议一键执行端点未建。

## 产品通视角：新功能 PRD 包（v0.1）
- 用户要求写"新功能 PRD"，未指定功能。经澄清：用户决策 = **砍 G3 同行激励榜**（隐私优先，排行榜暴露相对经营位置，与"私人商户数据私密"冲突）；**G1/G2/G4 给完整 PRD**；**G8/G9/G10 给规格要点**（需独立深化）；并主动补盲"未想到的需求"。
- 产出 `E:\千摊\千摊智脑-新功能PRD包-v0.1.md`：
  - G1 多商户登录鉴权（P0，安全地基，先于离线同步；含 N2 隐私同意）
  - G2 收款台后端（P0，与离线同步 D1 合并；client_id 幂等复用）
  - G4 经营日历/时令后端（P1，复用 weather.py）
  - 补盲 N1–N7：数据所有权/被遗忘权、隐私同意、多角色、风控预警、适老化、备份恢复、审计可视
  - 隐私横切要求（§6）+ 发布计划依赖总览（§7）
- 关键产品判断：以"商户数据私密"为透镜贯穿全文档，G3 正式砍除并留痕。

## 续：PRD 包 v0.2（用户"都做 + 再挖"）
- 产出 `E:\千摊\千摊智脑-新功能PRD包-v0.2.md`（v0.1 续作）：
  - A) G1 鉴权技术方案：接口定义(login/refresh/logout/me)、数据模型变更(Merchant.openid + auth_refresh_tokens + consent_records)、路由改造清单(9 个 router 去客户端 merchant_id)、安全要点（接地 `千摊智脑-auth参考实现.py`）
  - B) 合规底线 PRD：N2 隐私同意(P0) + N1 数据所有权/被遗忘权(P1，导出/删除/冷静期)
  - C) G8 AI经营问答 / G9 预测评估看板 / G10 Web经营后台 三个完整独立 PRD
  - D) 二次补盲 N8–N20（自我里程碑替代G3、多店、应收应付、现金流预警、损耗根因、新手引导、帮助中心、方言、营销⚠️、税务、设备迁移、暗黑模式），标 ⚠️ 需隐私审查项
  - E) 合并路线图总览
- 用户偏好确认：隐私优先、社交/曝光类需求敏感；PM 应主动补盲并标注隐私风险。
