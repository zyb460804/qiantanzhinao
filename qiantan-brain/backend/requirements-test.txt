# 测试依赖（deploy.sh cmd_test 在部署机上安装；CI 走 requirements-dev.txt）。
# 版本须与 requirements-dev.txt 对齐，避免本地 / CI / 部署三环境漂移。
# 运行时依赖（aiosqlite / httpx / bcrypt 等）由 requirements.txt 提供，此处不重复。

-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==5.0.0
types-redis==4.6.0.20241004
