# Development & testing dependencies.
# Install with:  pip install -r requirements-dev.txt
# (pulls in the base runtime requirements from requirements.txt automatically)

-r requirements.txt

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0

# Coverage reporting (optional — uncomment to use `pytest --cov`):
# pytest-cov==5.0.0

# ── 代码质量门禁（团队技术能力提升方案）──
# ruff: lint + 格式化（替代 flake8/isort/black 三件套，单一工具、极快）
ruff==0.6.9
# mypy: 类型检查，配合 pyproject.toml 的 [tool.mypy]
mypy==1.11.2
