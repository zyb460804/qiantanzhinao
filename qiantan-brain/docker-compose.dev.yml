# 本地开发端口映射；生产环境不要加载本文件。
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"
  redis:
    ports:
      - "127.0.0.1:6379:6379"
  backend:
    ports:
      - "127.0.0.1:8000:8000"

networks:
  private:
    internal: false
