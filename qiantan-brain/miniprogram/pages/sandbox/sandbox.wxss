.sandbox-page { padding-top: 22rpx; }
.sandbox-hero { background: var(--green-950); }
.lab-steps { position: relative; z-index: 2; display: flex; gap: 20rpx; margin-top: 30rpx; padding-top: 20rpx; border-top: 1rpx solid rgba(255,255,255,.13); color: rgba(255,255,255,.45); font-size: 22rpx; }
.lab-steps .active { color: #f6ca78; font-weight: 700; }
.config-panel { padding: 28rpx; background: var(--paper); border-radius: var(--radius-lg); box-shadow: var(--shadow); }
.field-row { display: flex; align-items: center; justify-content: space-between; padding-bottom: 26rpx; border-bottom: 1rpx solid var(--line); }
.field-label, .field-help { display: block; }
.field-label { color: var(--ink); font-size: 26rpx; font-weight: 700; }
.field-help { margin-top: 3rpx; color: var(--muted); font-size: 22rpx; }
.picker-value { display: flex; align-items: center; gap: 14rpx; min-width: 210rpx; padding: 16rpx 18rpx; color: var(--green-800); background: var(--green-50); border-radius: 15rpx; font-size: 25rpx; font-weight: 700; }
.picker-value > text { margin-left: auto; }
.field-block { padding: 28rpx 0; border-bottom: 1rpx solid var(--line); }
.slider-head { display: flex; align-items: flex-end; justify-content: space-between; }
.quantity-value { color: var(--green-800); font-size: 42rpx; font-weight: 800; }
.quantity-value > text { font-size: 22rpx; font-weight: 500; }
.field-block slider { margin: 22rpx 0 0; }
.range-label { display: flex; justify-content: space-between; color: var(--muted); font-size: 22rpx; }
.price-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16rpx; padding-top: 26rpx; }
.price-field { min-width: 0; }
.money-input { display: flex; align-items: center; margin-top: 12rpx; padding: 18rpx; background: var(--paper-2); border: 1rpx solid var(--line); border-radius: 16rpx; color: var(--green-800); font-size: 25rpx; font-weight: 700; }
.money-input input { flex: 1; min-width: 0; margin-left: 7rpx; color: var(--ink); font-size: 29rpx; }
.money-input .unit { color: var(--muted); font-size: 22rpx; font-weight: 500; }
.run-button { display: flex; align-items: center; justify-content: center; gap: 14rpx; min-height: 96rpx; margin-top: 28rpx; color: #fff; background: var(--green-800); border-radius: 20rpx; font-size: 29rpx; font-weight: 750; box-shadow: 0 12rpx 26rpx rgba(0, 160, 106,.22); }
.run-mark { display: flex; align-items: center; justify-content: center; width: 40rpx; height: 40rpx; color: var(--green-950); background: #f6ca78; border-radius: 12rpx; font-size: 22rpx; font-weight: 800; }
/* P0-2 UI 面：试算失败持久错误态 */
.sim-error { padding: 34rpx 30rpx; background: var(--paper); border: 1rpx solid #f3d9d5; border-radius: var(--radius-lg); box-shadow: var(--shadow); }
.sim-error-title { display: block; font-size: 30rpx; font-weight: 750; color: #b3362a; }
.sim-error-desc { display: block; margin-top: 8rpx; font-size: 24rpx; line-height: 1.6; color: var(--ink-2); }
.sim-error .btn-sm { margin-top: 20rpx; }
.profit-card { padding: 30rpx; background: var(--paper); border-radius: var(--radius-lg); box-shadow: var(--shadow); }
.profit-label { display: block; color: var(--muted); font-size: 22rpx; }
.profit-number { display: block; margin-top: 3rpx; font-size: 60rpx; line-height: 1.25; font-weight: 820; letter-spacing: -2rpx; }
.profit-grid { display: grid; grid-template-columns: repeat(3, 1fr); margin-top: 24rpx; padding: 22rpx 0; background: var(--paper-2); border-radius: 18rpx; }
.profit-grid > view { padding: 0 14rpx; text-align: center; border-right: 1rpx solid var(--line); }
.profit-grid > view:last-child { border-right: 0; }
.profit-grid text { display: block; }
.profit-grid text:first-child { color: var(--ink); font-size: 27rpx; font-weight: 750; }
.profit-grid text:last-child { margin-top: 4rpx; color: var(--muted); font-size: 22rpx; }
.cost-lines { margin-top: 20rpx; }
.cost-lines > view { display: flex; justify-content: space-between; padding: 10rpx 0; color: var(--ink-2); font-size: 23rpx; }
.recommendation { display: flex; align-items: flex-start; gap: 14rpx; margin-top: 18rpx; padding: 18rpx; color: var(--green-800); background: var(--green-50); border-radius: 16rpx; font-size: 23rpx; }
.rec-mark { flex: 0 0 auto; display: flex; align-items: center; justify-content: center; width: 34rpx; height: 34rpx; color: #fff; background: var(--green-700); border-radius: 10rpx; font-size: 22rpx; font-weight: 800; }
.compare-card { padding: 26rpx; background: var(--paper); border-radius: var(--radius-lg); box-shadow: var(--shadow); }
/* 方案净收益横条（纯 CSS，替代原 canvas 柱图） */
.scheme-bars { display: flex; flex-direction: column; gap: 16rpx; margin-bottom: 20rpx; }
.scheme-bar-row { display: flex; align-items: center; gap: 16rpx; }
.scheme-bar-name { flex: 0 0 110rpx; color: var(--ink-2); font-size: 23rpx; }
.scheme-bar-track { flex: 1; height: 16rpx; overflow: hidden; background: var(--paper-3); border-radius: var(--r-pill); }
.scheme-bar-fill { height: 100%; border-radius: var(--r-pill); background: var(--green-600); transition: width var(--dur-slow) var(--ease-expo); }
.scheme-bar-fill.is-neg { background: var(--tomato); }
.scheme-bar-fill.is-best { background: var(--green-800); box-shadow: 0 0 0 4rpx var(--green-100); }
.scheme-bar-val { flex: 0 0 auto; min-width: 110rpx; text-align: right; font-family: var(--font-num); font-size: 24rpx; font-weight: 800; font-variant-numeric: tabular-nums; }
.compare-table { overflow: hidden; border: 1rpx solid var(--line); border-radius: 16rpx; }
.table-row { display: grid; grid-template-columns: 1.15fr 1fr 1fr 1fr; padding: 18rpx 14rpx; border-bottom: 1rpx solid var(--line); color: var(--ink-2); font-size: 22rpx; text-align: right; }
.table-row:last-child { border-bottom: 0; }
.table-row text:first-child { text-align: left; }
.table-head { color: var(--muted); background: var(--paper-2); font-size: 22rpx; font-weight: 650; }
.scheme-name { color: var(--ink); font-weight: 700; }
